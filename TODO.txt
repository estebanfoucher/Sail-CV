Refacto :


    - refactor so mast3r is docked to the codebase : create a class densepointmatchermodel.
        - means need to change mast3r_engine to eat images without loading them

    - fp16 model

    - clean les log debug dans loguru et mettre le level par defaut a info via .env

    - option : pydantic BaseModel refactor input-outputs de modeles, images, to make a real webapp
